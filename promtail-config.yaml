server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: product-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: product-service
          service: ProductService
          environment: production
          __path__: /app/logs/productservice-*.log
          
    pipeline_stages:
      - json:
          expressions:
            level: '"@l"'
            message: '"@mt"'
            ts: '"@t"'
            
      # Default to Information if level is missing      
      - template:
          source: level
          template: '{{ if .Value }}{{ .Value }}{{ else }}Information{{ end }}'    

      - labels:
          level:

      - timestamp:
          source: ts
          format: RFC3339 
          
  - job_name: order-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: order-service
          service: OrderService
          environment: production
          __path__: /app/logs/orderservice-*.log
          
    pipeline_stages:
      - json:
          expressions:
            level: '"@l"'
            message: '"@mt"'
            ts: '"@t"'
            
      # Default to Information if level is missing      
      - template:
          source: level
          template: '{{ if .Value }}{{ .Value }}{{ else }}Information{{ end }}'    

      - labels:
          level:

      - timestamp:
          source: ts
          format: RFC3339
  
  - job_name: user-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: user-service
          service: UserService
          environment: production
          __path__: /app/logs/userservice-*.log
          
    pipeline_stages:
      - json:
          expressions:
            level: '"@l"'
            message: '"@mt"'
            ts: '"@t"'
            
      # Default to Information if level is missing      
      - template:
          source: level
          template: '{{ if .Value }}{{ .Value }}{{ else }}Information{{ end }}'    

      - labels:
          level:

      - timestamp:
          source: ts
          format: RFC3339
          
  - job_name: payment-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: payment-service
          service: PaymentService
          environment: production
          __path__: /app/logs/paymentservice-*.log
          
    pipeline_stages:
      - json:
          expressions:
            level: '"@l"'
            message: '"@mt"'
            ts: '"@t"'
            
      # Default to Information if level is missing      
      - template:
          source: level
          template: '{{ if .Value }}{{ .Value }}{{ else }}Information{{ end }}'    

      - labels:
          level:

      - timestamp:
          source: ts
          format: RFC3339
          
  - job_name: object-store-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: object-store-service
          service: ObjectStoreService
          environment: production
          __path__: /app/logs/objectstoreservice-*.log
          
    pipeline_stages:
      - json:
          expressions:
            level: '"@l"'
            message: '"@mt"'
            ts: '"@t"'
            
      # Default to Information if level is missing      
      - template:
          source: level
          template: '{{ if .Value }}{{ .Value }}{{ else }}Information{{ end }}'    

      - labels:
          level:

      - timestamp:
          source: ts
          format: RFC3339        
          
